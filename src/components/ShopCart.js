import React from 'react'

const ShopCart = ({numCart}) => {    

    return (
        <div>
            <h3>contador compras: {numCart}</h3>            
        </div>
    )
}

export default ShopCart
